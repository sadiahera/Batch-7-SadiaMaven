package variableTypePackage;

public class VariableType {
    /* There are three types of variables:
    1.Instance/Global Variables -- The variables declares inside the class are called
    Instance variables.
    2. Local variables -- The variables declare inside the methods , are called
    local variables . it's born inside the methods and die inside the methods. it has
    no capacity to go outside.
    3. Static variables --
 */

    byte age = 100 ;   // we both declare and intialize/define the variables
    short CheckingACCount ; // we declare the variables .


    public void study() {  // Function -- Method can hold variables , statements

        System.out.println(age);
        CheckingACCount = 32767; // initialize/define the variables
        System.out.println(CheckingACCount);
        boolean snoringInTheClass = true;






    }










}
